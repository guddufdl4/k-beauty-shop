/**
 * Browser Supabase client — Phase 2 implementation
 * Requires: @supabase/supabase-js, @supabase/ssr
 * Env: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
 */

export function createBrowserClient() {
  throw new Error(
    "Supabase browser client is not configured. Complete setup in Phase 2."
  );
}
